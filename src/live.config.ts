// import { defineLiveCollection } from "astro:content";
// import { paymentLinkLoader, productLoader } from "./loaders/stripe";

// const products = defineLiveCollection({ loader: productLoader() });
// const paymentLinks = defineLiveCollection({ loader: paymentLinkLoader() });

// export const collections = { products, paymentLinks };
